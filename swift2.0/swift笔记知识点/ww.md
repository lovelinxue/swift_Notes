###打包程序生成IPA文件
- ####第一种:正常打包方法
    - 做完要构建版本打包的前置工作(版本号修改等等...)
    - Xcdoe的菜单栏 -> `Product` -> `Archive`开始打包生成IPA
    - 完成之后弹出`Organizer`界面,如果要上传商店可以直接点击右边的`Upload to App Store` 
    - 如果要导出IPA文件,可以点击右边

